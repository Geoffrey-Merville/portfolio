import SectionArticle from "./SectionArticle";

const ContentSection = ({sectionId}) => {

  return (
    <div id={sectionId}>
      <SectionArticle />
    </div>
  );
};

export default ContentSection;
